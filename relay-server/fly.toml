app = "puck-duel-relay"
primary_region = "sin"  # Singapore — change to region closest to your users

[build]

[http_service]
  internal_port = 4000
  force_https = false
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[env]
  PORT = "4000"
